import { createPlaceholderRoute } from "@/app/lib/placeholder-route";

const route = createPlaceholderRoute("/products/ai-native-software-factory");
export const metadata = route.metadata;
export default route.Page;
